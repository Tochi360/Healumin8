# BaseHub token retrieved here: https://basehub.com
BASEHUB_TOKEN=""

# Resend (for Request a demo form → Hello@healumin8.com). Get key: https://resend.com/api-keys
RESEND_API_KEY=""
# Optional. Sender address (must be a verified domain in Resend). Default: onboarding@resend.dev
# RESEND_FROM_EMAIL="Healumin8 Website <noreply@healumin8.com>"

# BaseHub workflow webhook (optional): receive demo form in BaseHub and email via /api/basehub-demo-webhook.
# Create a Workflow in BaseHub with trigger "New events in Event Block" → your form's Event Block,
# set Webhook URL to https://yourdomain.com/api/basehub-demo-webhook, then paste the Webhook Secret here.
# BASEHUB_DEMO_WEBHOOK_SECRET=""
